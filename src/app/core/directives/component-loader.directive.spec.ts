import { ComponentLoaderDirective } from './add-stepper-form.directive';

describe('AddStepperFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
