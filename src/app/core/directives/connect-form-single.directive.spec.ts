import { ConnectFormSingleDirective } from './connect-form-single.directive';

describe('ConnectFormSingleDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectFormSingleDirective();
    expect(directive).toBeTruthy();
  });
});
