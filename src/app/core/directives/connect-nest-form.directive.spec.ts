import { ConnectFormNestDirective } from './connect-nest-form.directive';

describe('ConnectFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectFormNestDirective();
    expect(directive).toBeTruthy();
  });
});
